# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Chapter06/10_MOCK_API_Request.spec.ts >> Mock API Request using Playwright
- Location: tests/Chapter06/10_MOCK_API_Request.spec.ts:3:5

# Error details

```
Error: page.waitForResponse: Target page, context or browser has been closed
=========================== logs ===========================
waiting for response "*/**/api-mocking/api/v1/fruits"
============================================================
```

# Test source

```ts
  1  | import {test, expect} from '@playwright/test'
  2  | 
  3  | test('Mock API Request using Playwright', async({page}) => {
  4  |     await page.route('*/**/api-mocking/api/v1/fruits', async route => {         
  5  |         //page.route method is used to intercept the network request and mock the response, 
  6  |         // it takes 2 parameters, the first one is the URL pattern to match the request and the 
  7  |         // second one is a callback function that will be called when the request is intercepted.
  8  | 
  9  |         // The callback function takes a single parameter, which is an object that contains information about the intercepted request.
  10 |         //Mock API Request
  11 |         const json:any[] = [
  12 |             {
  13 |                 name: "Apple",
  14 |                 id: 12
  15 |             },
  16 |             {
  17 |                 name: "Orange",
  18 |                 id: 13
  19 |             },
  20 |             {
  21 |                 name: "Banana",
  22 |                 id: 14
  23 |             },
  24 |             {
  25 |                 name: "Strawberry",
  26 |                 id: 15
  27 |             },
  28 |             {
  29 |                 name: "Grapes",
  30 |                 id: 16
  31 |             }
  32 |         ]
  33 |         await route.fulfill({json});
  34 |     });
  35 | 
  36 |     // Navigate to the page that makes the API request
  37 |     await page.goto('https://playwright.dev/docs/mock-api');
  38 | 
  39 |     // Wait for the API request to complete and the response to be rendered on the page
> 40 |     await page.waitForResponse('*/**/api-mocking/api/v1/fruits');
     |                ^ Error: page.waitForResponse: Target page, context or browser has been closed
  41 |    
  42 |     //verify that the mocked response is displayed on the page
  43 | 
  44 |     expect(await page.getByText('Apple')).toBeVisible();
  45 |     expect(await page.getByText('Orange')).toBeVisible();
  46 |     expect(await page.getByText('Banana')).toBeVisible();
  47 |     expect(await page.getByText('Strawberry')).toBeVisible();
  48 |     expect(await page.getByText('Grapes')).toBeVisible();
  49 | 
  50 | });
```